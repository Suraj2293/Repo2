package net.paladion.model;

import kafka.serializer.Encoder;
import kafka.utils.VerifiableProperties;

public class ThreatEncoder implements Encoder<MultiEventCoreDTO> {

	@Override
	public byte[] toBytes(MultiEventCoreDTO arg0) {
		// return SerializationUtils.serialize(arg0);
		return null;
	}

	public ThreatEncoder(VerifiableProperties verifiableProperties) {
		/* This constructor must be present for successful compile. */
	}

}
